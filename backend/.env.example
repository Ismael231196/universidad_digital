# ──────────────────────────────────────────────
# Universidad Digital – Variables de entorno
# Copia este archivo como ".env" y rellena los valores reales.
# NUNCA subas el .env con credenciales reales al repositorio.
# ──────────────────────────────────────────────

# ── Aplicación ─────────────────────────────────
APP_ENV=development          # development | production
APP_DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/universidad
APP_JWT_SECRET=cambia_esto_por_un_secreto_seguro_de_32_caracteres
APP_JWT_EXPIRATION=60        # minutos
APP_COOKIE_SECURE=false      # true en producción (HTTPS)
APP_COOKIE_SAMESITE=lax      # lax | none (none en producción cross-site)
APP_CORS_ORIGINS=http://localhost:3000

# ── URL del frontend ───────────────────────────
# Se usa para construir el enlace de restablecimiento de contraseña
# Ejemplo: https://universidad-digital.vercel.app
FRONTEND_URL=http://localhost:3000

# ── Email – Brevo (SMTP) ───────────────────────
# Brevo (antes Sendinblue) es un proveedor de email DISTINTO a SendGrid.
# Usa sus propias credenciales SMTP; no son intercambiables con SendGrid.
#
# Dónde obtener las credenciales:
#   Brevo → ajustes → SMTP y API → Servidores SMTP → "Generar nueva contraseña SMTP"
#
BREVO_SMTP_HOST=smtp-relay.brevo.com
BREVO_SMTP_PORT=587
BREVO_SMTP_USER=tu_login_smtp@smtp-brevo.com   # Campo "Iniciar sesión" en Brevo
BREVO_SMTP_PASS=tu_contraseña_smtp_brevo        # Contraseña SMTP generada en Brevo

# Remitente que verán los destinatarios (debe estar verificado en Brevo)
EMAIL_FROM=noreply@tudominio.com
